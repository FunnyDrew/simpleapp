h1.mt-5
	= @post.title
.mt-3
	= @post.body
hr
= simple_form_for  @post.comments.build,method: :post,  url: post_comments_path(@post) do |c|
	=c.input :content
	=c.submit
/-get_hi
hr
/-debugger
- unless @post.comments.empty?
	= render partial: "comment", collection: @post.comments



/def has_child(node)
/  node.is_a?(Hash)
/end

/def print_nested_hash(hash)
/  hash.map do |key, value|
/    if has_child(value)
/      pp "we have nested hash there"
/      print_nested_hash(value)
/    else
/      pp "key is: #{key}"
/      pp "value is: #{value}"
/    end
/  end
/end
