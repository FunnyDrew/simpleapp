databases:
  - name: myDB1
    databaseName: mysite
    user: mysite
    plan: free

services:
  - type: web
    name: simpleapp
    runtime: ruby
    plan: free
    buildCommand: "./bin/render-build.sh"
    # preDeployCommand: "bundle exec rails db:migrate" # preDeployCommand only available on paid instance types
    startCommand: "bundle exec rails server"
    envVars:
      - key: postgresql://mysite_qtr5_user:akTr9FtWPJZp8O0BS0cARU2gUWO1UaCc@dpg-cpui6v6ehbks73eht060-a/mysite_qtr5
        fromDatabase:
          name: myDB1
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false
      - key: WEB_CONCURRENCY
        value: 2 # sensible default